---
# See: https://docs.codecov.io/docs/codecov-yaml
codecov:
  require_ci_to_pass: true

coverage:
  precision: 1
  round: down
  range: "70...100"

  status:
    project: true
    patch: true
    changes: false

comment:
  layout: "diff, files"
  behavior: default
  require_changes: true
